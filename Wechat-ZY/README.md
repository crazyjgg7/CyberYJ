# 易经六爻卜卦微信小程序 (Wechat-ZY)

基于原生微信小程序框架开发的六爻卜卦应用，融合了传统易经智慧与现代交互体验。

---

## 🌟 核心功能

1. **沉浸式起卦**: 
   - **重力感应**: 如同真实摇卦，支持 **加速度计感应起卦** (Gyroscope)。
   - **触觉反馈**: 使用 `wx.vibrate` 模拟真实投币与掷地的触感。
   - **六爻排盘**: 内置专业易经排盘算法，精准生成本卦、变卦。

2. **智能解卦 (MVP+Phase3)**:
   - **图文并茂**: 卦名、卦辞、象辞、卦符一目了然。
   - **多维分析**: 提供 **综合断语**、**五行生克**、**节气旺衰**、**宜忌建议**。
   - **API 对接**: 已接入 CyberYJ 后端 `POST /v1/divination/interpret`，网络失败时自动回退 Mock 数据。

3. **辅助功能 (Phase3+4)**:
   - **历史记录**: 自动保存卜卦结果至本地，支持列表回溯与详情查看。
   - **社交分享**: 一键生成精美 **分享海报** 保存至相册，或直接 **转发给好友**。

---

## 🚀 快速接入指南

### 1. 环境准备
- 下载并安装 [微信开发者工具 (Stable版)](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)。
- 申请一个微信小程序 **AppID** (若无正式ID，可点击测试号)。

### 2. 导入项目
1. 打开微信开发者工具。
2. 点击 **导入项目** (Import Project)。
3. **目录**: 选择本项目根目录 `/Users/apple/dev/CyberYJ/Wechat-ZY`。
4. **AppID**: 填入你的 AppID (或选择测试号)。
5. 点击 **确定**。

### 3. 本地预览与真机调试
- **模拟器**: 可预览首页、历史记录页的基本交互。
- **真机调试 (推荐)**: 
  - 点击工具栏的 **真机调试** 按钮 -> 二维码扫码。
  - **重要**: 只有真机才能体验完整的 **摇一摇起卦** (加速度传感器) 和 **生成海报保存相册** 功能。

### 4. 后端联调配置（必做）
1. 确认后端 HTTP API 已启动（默认 `http://127.0.0.1:18080`，接口 `POST /v1/divination/interpret`）。
2. 打开 `config/env.js`，按联调环境设置：
   - 本地调试：`currentEnv = 'dev'`
   - 小程序线上环境：`currentEnv = 'prod'`，并确保 `prod.baseUrl` 是 HTTPS 且已配置 request 合法域名。
3. `apiKey` 需与后端 `CYBERYJ_API_KEY` 一致。

### 5. 快速自检
- 导入项目后先确认 `project.config.json` 无报错（`sitemap.json` 已内置）。
- 进入“问卦 -> 结果页”，如果后端不可达会自动回退 Mock 数据并显示提示文案。
- 如需严格联调，可将 `config/env.js` 的 `useMock` 保持 `false`，确保所有请求都走真实后端。

---

## 📂 项目结构

```bash
Wechat-ZY/
├── pages/
│   ├── index/        # 首页 (功能入口)
│   ├── divination/   # 卜卦页 (摇一摇核心逻辑 + 动画)
│   ├── result/       # 结果页 (解卦展示 + 海报生成 Canvas)
│   └── history/      # 历史记录页 (本地存储列表)
├── services/
│   ├── api.js        # API 服务 (真实接口 + 失败回退 Mock)
│   └── gyro.js       # 陀螺仪传感器服务封装
├── utils/
│   ├── divination.js # 六爻排盘算法 (根据硬币计算卦象)
│   └── storage.js    # 本地数据持久化工具
├── components/       # 公共组件 (将来可抽离 Poster)
├── docs/             # 设计文档与开发计划
│   ├── api_requirements.md # 后端接口规范文档
│   └── plans/              # 各阶段实施计划
└── app.json          # 全局配置 (路由、窗口样貌)
```

## 📚 接口开发规范

如果您是后端开发者，请参考 `docs/api_requirements.md` 进行接口开发。
本项目已预留标准 HTTP 接口调用逻辑，支持两种响应格式：
- 直出业务结构：`{ hexagram, analysis, ... }`
- 包装协议结构：`{ tool, data, meta }`（前端自动解包 `data`）

---

> Created by CyberYJ Team with 🤖 AI Assistant (Antigravity)
